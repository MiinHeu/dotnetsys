<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="VinhKhanh.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:VinhKhanh.App"
    xmlns:res="clr-namespace:VinhKhanh.App.Resources.Strings"
    Title="Vinh Khanh"
    BackgroundColor="#FFFFFF"
    TabBarBackgroundColor="#FFFFFF"
    TabBarForegroundColor="#FF5A1F"
    TabBarTitleColor="#111827"
    TabBarUnselectedColor="#9CA3AF">


    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid Padding="0,5">
                <Label Text="{Binding Title}" HorizontalOptions="Center" VerticalOptions="Center"
                       FontAttributes="Bold" FontSize="14" TextColor="#111827" />
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <TabBar>
        <ShellContent x:Name="TabMap" Title="{x:Static res:AppResources.TabMap}" ContentTemplate="{DataTemplate local:MainPage}" Route="MainPage" />
        <ShellContent x:Name="TabPois" Title="{x:Static res:AppResources.TabPois}" ContentTemplate="{DataTemplate local:PoiListPage}" Route="PoiListPage" />
        <ShellContent x:Name="TabTours" Title="{x:Static res:AppResources.TabTours}" ContentTemplate="{DataTemplate local:ToursPage}" Route="Tours" />
        <ShellContent x:Name="TabChat" Title="{x:Static res:AppResources.TabChat}" ContentTemplate="{DataTemplate local:ChatPage}" Route="Chat" />
        <ShellContent x:Name="TabScan" Title="{x:Static res:AppResources.TabScan}" ContentTemplate="{DataTemplate local:QrScanPage}" Route="Qr" />
        <ShellContent x:Name="TabSettings" Title="{x:Static res:AppResources.TabSettings}" ContentTemplate="{DataTemplate local:SettingsPage}" Route="Settings" />
    </TabBar>
</Shell>
